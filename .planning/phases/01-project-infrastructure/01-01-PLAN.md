---
phase: 01-project-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mkdocs.yml
  - docs/index.md
  - docs/whats-changed.md
  - docs/game-design/pillars.md
  - docs/game-design/voice.md
  - docs/game-design/core-loop.md
  - docs/game-design/quests.md
  - docs/game-design/feedback.md
  - docs/dev-reference/architecture.md
  - docs/dev-reference/conventions.md
  - docs/dev-reference/contributing.md
  - docs/dev-reference/git-primer.md
  - docs/dev-reference/gsd-workflow.md
  - .github/workflows/docs.yml
  - .github/workflows/whats-changed.yml
  - scripts/generate_whats_changed.py
  - CHANGELOG.md
autonomous: true
requirements:
  - INFR-01
  - INFR-02
  - INFR-04

must_haves:
  truths:
    - "mkdocs build succeeds locally with zero errors"
    - "GitHub Actions docs workflow triggers on push to master"
    - "CHANGELOG.md exists with AI state journal format entry"
    - "whats-changed generation script runs without error when given an API key"
  artifacts:
    - path: "mkdocs.yml"
      provides: "MkDocs configuration with Material theme and full nav"
      contains: "name: material"
    - path: ".github/workflows/docs.yml"
      provides: "GitHub Pages deployment on push to master"
      contains: "deploy-pages@v4"
    - path: ".github/workflows/whats-changed.yml"
      provides: "Weekly whats-changed regeneration via Claude API"
      contains: "ANTHROPIC_API_KEY"
    - path: "scripts/generate_whats_changed.py"
      provides: "Claude API generation script adapted for Lanternkeeper"
      contains: "SYSTEM_PROMPT"
    - path: "CHANGELOG.md"
      provides: "AI state journal with first entry"
      contains: "State now:"
    - path: "docs/whats-changed.md"
      provides: "Whats-changed page with GENERATED marker"
      contains: "GENERATED CONTENT BELOW"
    - path: "docs/index.md"
      provides: "Docs site home page"
  key_links:
    - from: ".github/workflows/docs.yml"
      to: "mkdocs.yml"
      via: "mkdocs build in CI"
      pattern: "mkdocs build"
    - from: ".github/workflows/whats-changed.yml"
      to: "scripts/generate_whats_changed.py"
      via: "python3 invocation"
      pattern: "python3 scripts/generate_whats_changed.py"
    - from: "scripts/generate_whats_changed.py"
      to: "CHANGELOG.md"
      via: "file read"
      pattern: "CHANGELOG_PATH"

user_setup:
  - service: github-pages
    why: "Docs site deployment target"
    dashboard_config:
      - task: "Enable GitHub Pages"
        location: "Repo Settings > Pages > Source: GitHub Actions"
  - service: github-secrets
    why: "Claude API key for whats-changed generation"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "gh secret set ANTHROPIC_API_KEY (use key from ~/.config/secrets/anthropic-apis.sh)"
---

<objective>
Set up MkDocs infrastructure, GitHub Actions deployment pipeline, CHANGELOG with AI state journal format, and whats-changed auto-generation. This creates the skeleton that Plan 02 fills with content.

Purpose: Every subsequent change in this project will be transparently documented. The infrastructure must exist before content authoring begins.
Output: Buildable MkDocs site with stub pages, GitHub Actions workflows, CHANGELOG with first entry, whats-changed generation script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-infrastructure/01-CONTEXT.md
@.planning/phases/01-project-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MkDocs configuration, docs stubs, and GitHub Actions deploy workflow</name>
  <files>mkdocs.yml, docs/index.md, docs/whats-changed.md, docs/game-design/pillars.md, docs/game-design/voice.md, docs/game-design/core-loop.md, docs/game-design/quests.md, docs/game-design/feedback.md, docs/dev-reference/architecture.md, docs/dev-reference/conventions.md, docs/dev-reference/contributing.md, docs/dev-reference/git-primer.md, docs/dev-reference/gsd-workflow.md, .github/workflows/docs.yml</files>
  <read_first>
    /Users/dave/github/whitedoeinn/marketing-ops/.github/workflows/docs.yml
    /Users/dave/github/whitedoeinn/marketing-ops/mkdocs.yml
    /Users/dave/github/wdi-dave-roberts/lanternkeeper-emberfall/.planning/phases/01-project-infrastructure/01-RESEARCH.md
  </read_first>
  <action>
Create `mkdocs.yml` at repo root with this exact configuration (per D-05, D-06):

```yaml
site_name: "Lanternkeeper: Emberfall"
site_url: https://wdi-dave-roberts.github.io/lanternkeeper-emberfall/
repo_url: https://github.com/wdi-dave-roberts/lanternkeeper-emberfall
edit_uri: edit/master/docs/

theme:
  name: material
  palette:
    - scheme: slate
      primary: deep orange
      accent: amber
  features:
    - navigation.tabs
    - navigation.sections
    - navigation.top
    - search.suggest
    - search.highlight
    - toc.follow
    - content.action.edit

nav:
  - Home: index.md
  - "What's Changed": whats-changed.md
  - Game Design:
    - Design Pillars: game-design/pillars.md
    - "Aetherling's Voice": game-design/voice.md
    - Core Loop: game-design/core-loop.md
    - Micro-Quests: game-design/quests.md
    - Feedback Lines: game-design/feedback.md
  - Dev Reference:
    - Architecture: dev-reference/architecture.md
    - Conventions: dev-reference/conventions.md
    - Contributing: dev-reference/contributing.md
    - Git Primer: dev-reference/git-primer.md
    - GSD Workflow: dev-reference/gsd-workflow.md

markdown_extensions:
  - admonition
  - pymdownx.details
  - pymdownx.superfences
  - pymdownx.tasklist:
      custom_checkbox: true
  - toc:
      permalink: true
```

Create `.github/workflows/docs.yml` -- copy from marketing-ops but change branch to `master`:

```yaml
name: docs
on:
  push:
    branches:
      - master

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
      - run: echo "cache_id=$(date --utc '+%V')" >> $GITHUB_ENV
      - uses: actions/cache@v4
        with:
          key: mkdocs-material-${{ env.cache_id }}
          path: ~/.cache
          restore-keys: mkdocs-material-
      - run: pip install mkdocs-material
      - run: mkdocs build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

Create `docs/index.md` -- the docs site home page. Include YAML frontmatter with `title: Home` (three-dash delimiters), then the page body:

- H1: `# Lanternkeeper: Emberfall`
- Intro paragraph: "A calm, ritual-based mobile companion game for solo indie developers."
- Second paragraph: "A red panda named Aetherling walks alongside you through a world called Emberfall, offering emotion check-ins, micro-quests, and quiet encouragement. Never pressure."
- H2: `## What is this site?` with a list of the three nav sections (Game Design, Dev Reference, What's Changed) with brief descriptions
- H2: `## Quick Links` with links to pillars, core-loop, and contributing pages

Create `docs/whats-changed.md` with YAML frontmatter (`title: "What's Changed"`), an H1 heading, one intro sentence, then the GENERATED marker comment: `<!-- GENERATED CONTENT BELOW -- do not edit manually. Run scripts/generate_whats_changed.py to regenerate. -->` followed by a placeholder italic line: `*No generated entries yet. The first entry will appear after the CHANGELOG pipeline runs.*`

Create stub files for ALL remaining nav entries. Each stub must have a `# Title` heading and a one-line placeholder like `*Content coming in Plan 02.*` so `mkdocs build` does not fail on missing files. The stubs are:

- `docs/game-design/pillars.md` with heading `# Design Pillars`
- `docs/game-design/voice.md` with heading `# Aetherling's Voice`
- `docs/game-design/core-loop.md` with heading `# Core Loop`
- `docs/game-design/quests.md` with heading `# Micro-Quests`
- `docs/game-design/feedback.md` with heading `# Feedback Lines`
- `docs/dev-reference/architecture.md` with heading `# Architecture`
- `docs/dev-reference/conventions.md` with heading `# Conventions`
- `docs/dev-reference/contributing.md` with heading `# Contributing`
- `docs/dev-reference/git-primer.md` with heading `# Git Primer`
- `docs/dev-reference/gsd-workflow.md` with heading `# GSD Workflow`

Add `site/` to `.gitignore` if not already present (MkDocs build output directory).
  </action>
  <verify>
    <automated>cd /Users/dave/github/wdi-dave-roberts/lanternkeeper-emberfall && mkdocs build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - mkdocs.yml contains `name: material` and `branches:` section absent (that's in docs.yml)
    - mkdocs.yml nav lists exactly 13 pages across Home, What's Changed, Game Design (5), Dev Reference (5)
    - .github/workflows/docs.yml contains `branches:` with value `master` (not `main`)
    - .github/workflows/docs.yml contains `actions/deploy-pages@v4`
    - docs/whats-changed.md contains `<!-- GENERATED CONTENT BELOW`
    - docs/index.md contains `# Lanternkeeper: Emberfall`
    - All 10 stub files exist under docs/game-design/ and docs/dev-reference/
    - `mkdocs build` exits with code 0
    - `site/` appears in .gitignore
  </acceptance_criteria>
  <done>MkDocs builds successfully with all nav pages resolved. GitHub Actions deploy workflow exists targeting master branch. All stub pages exist so CI will not fail on first push.</done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG, whats-changed generation script, and generation workflow</name>
  <files>CHANGELOG.md, scripts/generate_whats_changed.py, .github/workflows/whats-changed.yml</files>
  <read_first>
    /Users/dave/github/whitedoeinn/marketing-ops/CHANGELOG.md
    /Users/dave/github/whitedoeinn/marketing-ops/scripts/generate_whats_changed.py
    /Users/dave/github/whitedoeinn/marketing-ops/.github/workflows/whats-changed.yml
    /Users/dave/github/wdi-dave-roberts/lanternkeeper-emberfall/.planning/phases/01-project-infrastructure/01-CONTEXT.md
  </read_first>
  <action>
Create `CHANGELOG.md` at repo root with the first entry using AI state journal format (per D-09). The file structure is:

- H1: `# Changelog`
- Intro paragraph: "AI state journal for Lanternkeeper: Emberfall. Each entry captures what changed, the resulting state, and constraints on future work. Reverse chronological."
- Link line: "For the human-readable narrative, see [What's Changed](https://wdi-dave-roberts.github.io/lanternkeeper-emberfall/whats-changed/) on the docs site."
- Horizontal rule separator
- H2: `## 2026-04-12`
- H3: `### Project Infrastructure -- Milestone Start`
- Three bullet points using the exact format:
  - `- **Changed:** Added MkDocs docs site with Material theme, GitHub Pages deployment, CHANGELOG, and reconciled CLAUDE.md. Corrected DOCS-01 requirement (no Godot content exists -- was a cross-project leak from laternfall). Renamed claude.md to CLAUDE.md.`
  - `- **State now:** Docs site live at https://wdi-dave-roberts.github.io/lanternkeeper-emberfall/. CHANGELOG.md is the running state journal. CLAUDE.md is the single source of truth for project constraints, pointing to src/data/ for game content and docs site for teaching reference. Game content (40 quests, 20 feedback lines) lives exclusively in src/data/.`
  - `- **Constrains:** Do not manually edit docs/whats-changed.md (generated). Do not duplicate quest/feedback content in CLAUDE.md. All future changes must satisfy at least one design pillar.`

Create `scripts/generate_whats_changed.py` -- adapt from marketing-ops (`/Users/dave/github/whitedoeinn/marketing-ops/scripts/generate_whats_changed.py`). The script structure is identical. Two changes from the marketing-ops version:

1. Replace the `SYSTEM_PROMPT` value. The new prompt must contain:
   - "Lanternkeeper: Emberfall, a calm companion game for solo indie developers"
   - "Allie (co-developer, learning developer)" as the audience
   - Rules: outcomes not operations, explain WHY, use "we", warm but not performative, avoid jargon, be concrete
   - Entry structure: #### heading title, 2-3 sentence body, **Why:** line
   - Skip instruction: pure docs restructuring, internal tooling, chore entries
   - End with: "Output ONLY the generated markdown entries. No preamble."

2. Replace the `USER_PROMPT_TEMPLATE` -- change the final instruction from "Start with the most recent week first" to "Start with the most recent date first" (Lanternkeeper uses date grouping, not week grouping).

Keep everything else identical: `REPO_ROOT`, `CHANGELOG_PATH`, `WHATS_CHANGED_PATH`, `GENERATED_MARKER = "<!-- GENERATED CONTENT BELOW"`, `read_static_header()`, `generate_narrative()` using `model="claude-sonnet-4-20250514"` with `max_tokens=8192`, and `main()`.

Create `.github/workflows/whats-changed.yml` -- copy from marketing-ops (`/Users/dave/github/whitedoeinn/marketing-ops/.github/workflows/whats-changed.yml`) exactly. No branch changes needed (this workflow uses cron + workflow_dispatch, not push triggers). Key elements that must be present:
- `cron: '0 13 * * 0'` (Sunday 1 PM UTC)
- `workflow_dispatch:` for manual trigger
- `permissions: contents: write`
- `if: github.actor != 'github-actions[bot]'` guard
- Step to check for changelog changes since last generation
- `ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}` env var
- Commit step with `github-actions[bot]` as author and `[skip ci]` in message
  </action>
  <verify>
    <automated>cd /Users/dave/github/wdi-dave-roberts/lanternkeeper-emberfall && python3 -c "import ast; ast.parse(open('scripts/generate_whats_changed.py').read()); print('Script parses OK')" && grep -c 'State now:' CHANGELOG.md && grep -c 'ANTHROPIC_API_KEY' .github/workflows/whats-changed.yml</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md first line is `# Changelog`
    - CHANGELOG.md contains `## 2026-04-12` date heading
    - CHANGELOG.md contains `- **Changed:**`, `- **State now:**`, `- **Constrains:**` in the first entry
    - CHANGELOG.md contains link to `https://wdi-dave-roberts.github.io/lanternkeeper-emberfall/whats-changed/`
    - scripts/generate_whats_changed.py parses without syntax errors (python3 -c "ast.parse(...)")
    - scripts/generate_whats_changed.py contains `SYSTEM_PROMPT` with "Lanternkeeper" (not "White Doe Inn")
    - scripts/generate_whats_changed.py contains `GENERATED_MARKER = "<!-- GENERATED CONTENT BELOW"`
    - scripts/generate_whats_changed.py uses model `claude-sonnet-4-20250514`
    - .github/workflows/whats-changed.yml contains `cron: '0 13 * * 0'`
    - .github/workflows/whats-changed.yml contains `ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}`
    - .github/workflows/whats-changed.yml contains `github-actions[bot]` for git config
  </acceptance_criteria>
  <done>CHANGELOG.md has first AI state journal entry. Generation script is syntactically valid and adapted for Lanternkeeper audience. GitHub Actions workflow triggers weekly and on manual dispatch with changelog-change detection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner to Claude API | Workflow sends CHANGELOG content to external API |
| GitHub Actions runner to GitHub Pages | Workflow deploys static site to public URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | ANTHROPIC_API_KEY in workflow | mitigate | Secret stored in GitHub repository secrets, accessed via `${{ secrets.ANTHROPIC_API_KEY }}`, never logged or committed |
| T-01-02 | Tampering | docs.yml workflow permissions | mitigate | Workflow uses minimal permissions: `contents: read`, `pages: write`, `id-token: write`. No write access to repo contents. |
| T-01-03 | Tampering | whats-changed.yml bot commits | accept | Bot commits are [skip ci] tagged and limited to `docs/whats-changed.md` only. Low risk: generated content, not code. |
| T-01-04 | Elevation of Privilege | Workflow trigger scope | mitigate | docs.yml triggers only on push to master. whats-changed.yml has `if: github.actor != 'github-actions[bot]'` guard to prevent recursive triggers. |
</threat_model>

<verification>
1. `mkdocs build` exits 0 with no warnings about missing files
2. All 13 nav pages resolve (no "Documentation file not found" errors)
3. CHANGELOG.md follows AI state journal format (Changed/State now/Constrains)
4. `python3 -c "ast.parse(open('scripts/generate_whats_changed.py').read())"` exits 0
5. Both GitHub Actions workflow files contain correct branch (`master`)
</verification>

<success_criteria>
- MkDocs builds locally without errors
- GitHub Actions docs.yml workflow file exists and targets master
- CHANGELOG.md exists with first AI state journal entry
- Generation script is syntactically valid and adapted for Lanternkeeper
- All nav-referenced stub pages exist
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-infrastructure/01-01-SUMMARY.md`
</output>
