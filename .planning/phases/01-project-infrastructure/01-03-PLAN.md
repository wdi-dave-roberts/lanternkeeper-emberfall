---
phase: 01-project-infrastructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - claude.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "CLAUDE.md is a single reconciled file with terse design constraints"
    - "No Godot or laternfall references exist in planning artifacts"
    - "Quest and feedback content is NOT duplicated in CLAUDE.md"
    - "GSD comment blocks are intact and at the end of the file"
    - "claude.md (lowercase) no longer exists as a separate file"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Single source of truth for project constraints"
      contains: "Design Pillars"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected DOCS-01 requirement text"
  key_links:
    - from: "CLAUDE.md"
      to: "src/data/quests.ts"
      via: "file reference pointer"
      pattern: "src/data/quests.ts"
    - from: "CLAUDE.md"
      to: "src/data/feedback.ts"
      via: "file reference pointer"
      pattern: "src/data/feedback.ts"
---

<objective>
Reconcile CLAUDE.md from Allie's claude.md -- slim to terse constraints, remove content catalogs, add pointers to src/data/ and docs site. Fix all laternfall/Godot references in planning artifacts. Rename claude.md to CLAUDE.md.

Purpose: CLAUDE.md becomes the enforceable single source of truth. Full teaching content lives on the docs site. Planning artifacts are clean of cross-project contamination.
Output: Reconciled CLAUDE.md, corrected REQUIREMENTS.md, corrected PROJECT.md, corrected ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-infrastructure/01-CONTEXT.md
@.planning/phases/01-project-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile CLAUDE.md and fix laternfall/Godot references in planning artifacts</name>
  <files>CLAUDE.md, claude.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md</files>
  <read_first>
    /Users/dave/github/wdi-dave-roberts/lanternkeeper-emberfall/CLAUDE.md
    /Users/dave/github/wdi-dave-roberts/lanternkeeper-emberfall/.planning/REQUIREMENTS.md
    /Users/dave/github/wdi-dave-roberts/lanternkeeper-emberfall/.planning/PROJECT.md
    /Users/dave/github/wdi-dave-roberts/lanternkeeper-emberfall/.planning/ROADMAP.md
    /Users/dave/github/wdi-dave-roberts/lanternkeeper-emberfall/.planning/phases/01-project-infrastructure/01-CONTEXT.md
  </read_first>
  <action>
**Part A: Reconcile CLAUDE.md** (per D-01, D-02, D-03, D-04)

The current `CLAUDE.md` file (originally `claude.md`, already uppercase via GSD tooling) contains the full quest catalog (40 quests) and full feedback lines (20 lines). These must be removed per D-03. The file must be slimmed to terse constraints only per D-02.

Rewrite the content ABOVE the first `<!-- GSD:project-start -->` marker. Do NOT modify anything between GSD comment markers -- leave all `<!-- GSD:*-start -->` through `<!-- GSD:*-end -->` blocks exactly as they are.

The new content above the GSD blocks should contain exactly these sections in this order. Use horizontal rule (`---`) as a separator between each section:

**Section 1 -- Header:**
H1: `# Lanternkeeper: Emberfall -- Claude Guide`
Blockquote line 1: `> Single source of truth for project constraints.`
Blockquote line 2: `> Full teaching reference: https://wdi-dave-roberts.github.io/lanternkeeper-emberfall/`

**Section 2 -- Project Summary:**
H2: `## Project Summary`
Text: "Lanternkeeper: Emberfall is a calm, ritual-based mobile companion game built with Expo + React Native. A red panda named Aetherling walks alongside the player through Emberfall, offering emotion check-ins, micro-quests, and quiet encouragement. Never pressure."
Second paragraph: "Built as a gift for Garrett (solo indie game dev building Atlas), and as a bonding/learning project between Dave and Allie."

**Section 3 -- Tech Stack:**
H2: `## Tech Stack`
Four bullet points:
- `Expo 54 + React Native + TypeScript`
- `Expo Router (file-based routing)`
- `AsyncStorage (local-only, no backend)`
- `React Native Reanimated + Gesture Handler (animations)`

**Section 4 -- Design Pillars:**
H2: `## Design Pillars`
Numbered list:
1. `**Understood** -- The app mirrors the user's inner state without judgment`
2. `**Inspired** -- The app protects creative momentum and meaning`
3. `**Less Pressured** -- No streaks, no penalties, time is neutral, rest is normal`

**Section 5 -- Core Loop:**
H2: `## Core Loop`
Numbered list (6 steps):
1. Home screen shows Aetherling and Emberfall scene
2. User clears fog and brushes leaves
3. User selects emotion: Stuck / Frustrated / Inspired / Alright
4. App suggests one micro-quest
5. User taps Done
6. Aetherling shows a feedback line

**Section 6 -- Aetherling Voice Rules:**
H2: `## Aetherling Voice Rules`
Line: `Tone: Calm, dry, warm-under-the-surface, respectful, non-performative`
Subheading or bold: `Language constraints:` followed by 4 bullets:
- Short sentences
- No exclamation points
- No slang or emojis
- No motivational cliches

Two identity lines:
- "Aetherling is a quiet builder, a steady companion, a thoughtful observer."
- "Aetherling is not a cheerleader, a therapist, or a productivity coach."

Link: `Full voice reference: https://wdi-dave-roberts.github.io/lanternkeeper-emberfall/game-design/voice/`

**Section 7 -- Game Content:**
H2: `## Game Content`
Three lines pointing to source files:
- `Quest catalog (40 quests): \`src/data/quests.ts\``
- `Feedback lines (20 lines): \`src/data/feedback.ts\``
- `Type definitions: \`src/data/types.ts\``
Important line: `Do not duplicate game content in this file. The source of truth is \`src/data/\`.`

**Section 8 -- Implementation Rules:**
H2: `## Implementation Rules`
Four bullets (same as current):
- Make one small change at a time
- Never refactor the whole project without permission
- Stop after each major step and ask for confirmation
- Keep visuals simple until the logic is solid

**Section 9 -- File Structure:**
H2: `## File Structure`
Code block (no language tag) showing directory tree:
  app/              # Expo Router screens and layouts
  src/components/   # Reusable UI components
  src/screens/      # Screen-level components
  src/data/         # Game content (quests, feedback, types)
  src/logic/        # Game logic
  src/storage/      # Storage utilities (legacy -- migrating to lib/)
  lib/              # Current storage abstraction

**Section 10 -- Final Rule:**
H2: `## Final Rule`
Text: "If a change does not make the user feel:"
Three bullets: more understood, more inspired, less pressured
Text: "Then do not build it."

After the Final Rule section, leave a blank line, then the existing `<!-- GSD:project-start -->` block begins. Everything from that marker onward stays unchanged.

**File rename:** Delete the old `claude.md` file if it still exists as a separate file from `CLAUDE.md`. On case-insensitive filesystems (macOS), `claude.md` and `CLAUDE.md` may be the same file. Use `git mv` if needed to ensure Git tracks the rename: `git mv claude.md CLAUDE.md` (if they are distinct) or verify the current filename casing in Git with `git ls-files claude.md CLAUDE.md`.

**Part B: Fix laternfall/Godot references in planning artifacts** (per D-14)

1. `.planning/REQUIREMENTS.md` line 31 -- change:
   FROM: `- [ ] **DOCS-01**: Reconcile CLAUDE.md files -- merge Dave's Godot architecture docs with Allie's design pillars and Aetherling voice rules`
   TO: `- [ ] **DOCS-01**: Reconcile CLAUDE.md -- slim to terse constraints, remove content catalogs, add pointers to src/data/ and docs site`

2. `.planning/PROJECT.md` -- find the line containing "merge Dave's Godot architecture docs" and change it to:
   `- [ ] Reconcile CLAUDE.md -- slim to terse constraints, remove content catalogs, add pointers to src/data/ and docs site`

3. `.planning/ROADMAP.md` line 32 -- change:
   FROM: `4. CLAUDE.md is a single reconciled file -- Godot architecture docs removed, design pillars and voice rules are the canonical source`
   TO: `4. CLAUDE.md is a single reconciled file -- terse constraints only, content catalogs moved to src/data/ and docs site`

Do NOT modify files in `.planning/phases/01-project-infrastructure/` (CONTEXT.md, RESEARCH.md, DISCUSSION-LOG.md) -- those are historical records that correctly document the decision trail including the discovery of the Godot leak.
  </action>
  <verify>
    <automated>cd /Users/dave/github/wdi-dave-roberts/lanternkeeper-emberfall && test -f CLAUDE.md && ! grep -i "godot" .planning/REQUIREMENTS.md && ! grep -i "godot" .planning/ROADMAP.md && ! grep -i "godot" .planning/PROJECT.md && grep "src/data/quests.ts" CLAUDE.md && grep "src/data/feedback.ts" CLAUDE.md && grep -c "GSD:project-start" CLAUDE.md && echo "All checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md exists (uppercase)
    - CLAUDE.md contains `# Lanternkeeper: Emberfall` as first heading
    - CLAUDE.md contains `src/data/quests.ts` as a pointer (not the full quest list)
    - CLAUDE.md contains `src/data/feedback.ts` as a pointer (not the full feedback list)
    - CLAUDE.md contains `Do not duplicate game content in this file`
    - CLAUDE.md does NOT contain "Open your project for 2 minutes" (first quest text -- proves catalogs removed)
    - CLAUDE.md does NOT contain "Fog lost a little ground" (first feedback text -- proves catalogs removed)
    - CLAUDE.md contains `<!-- GSD:project-start` marker (GSD blocks preserved)
    - CLAUDE.md contains `<!-- GSD:conventions-start` marker (GSD blocks preserved)
    - CLAUDE.md contains the Final Rule section with "more understood", "more inspired", "less pressured"
    - .planning/REQUIREMENTS.md line for DOCS-01 does NOT contain "Godot"
    - .planning/PROJECT.md does NOT contain "Godot"
    - .planning/ROADMAP.md Phase 1 success criteria #4 does NOT contain "Godot"
    - `git ls-files CLAUDE.md` returns `CLAUDE.md` (file tracked with correct casing)
  </acceptance_criteria>
  <done>CLAUDE.md is reconciled: terse constraints, no content catalogs, pointers to src/data/ and docs site, GSD blocks intact. All planning artifacts (REQUIREMENTS.md, PROJECT.md, ROADMAP.md) are free of Godot/laternfall references. Historical phase artifacts (CONTEXT.md, RESEARCH.md) preserved unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan modifies documentation files only -- no code, no secrets, no external services |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | CLAUDE.md content | accept | File contains project design constraints only. No secrets, no PII. Repo is public. |
| T-03-02 | Tampering | GSD comment markers | mitigate | Plan explicitly instructs not to modify GSD blocks. Acceptance criteria verify marker presence. |
</threat_model>

<verification>
1. CLAUDE.md is terse -- no quest catalog, no feedback catalog
2. CLAUDE.md points to src/data/ for game content
3. GSD comment blocks are intact
4. No Godot/laternfall references in REQUIREMENTS.md, PROJECT.md, or ROADMAP.md
5. Historical phase artifacts are unchanged
</verification>

<success_criteria>
- CLAUDE.md is the single reconciled source of truth: terse constraints + pointers
- Full content catalogs (40 quests, 20 feedback lines) are gone from CLAUDE.md
- All planning artifacts corrected for Godot/laternfall cross-contamination
- GSD tooling blocks preserved intact
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-infrastructure/01-03-SUMMARY.md`
</output>
